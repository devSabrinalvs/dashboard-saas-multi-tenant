# Test database — aponta para um DB separado no mesmo container Postgres.
# Não compartilha dados com o banco de desenvolvimento.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/saas_multitenant_test?schema=public"
